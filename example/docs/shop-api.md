# Shop API (fixture)

This file stands in for the real documentation of the fictional shop
system. The example claims point their verify commands at it.

- Scopes granted to the read token: read_orders, read_customers, read_products.
- Order timelines run over `processed_at`, not `created_at`.
